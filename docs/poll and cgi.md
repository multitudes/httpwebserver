what would be the logic - can you make a sort of diagram? 
i have a poll...  and a client which starts a fork and adds 2 fds, one for cgiin and one for cgi out.
after creating these i go back to the poll...
so i need to first write to the cgi_in the first part of the body i had when i got the headers if any...
so i check if the cgi_in fd is pollout looping on them... 
then write the content of the buffer to the client substracting the headers and then i probably should read from the client again. for that i need to check the client is polliin and fill the buffer again qith BUFFER_SIZE. and repeat until when i get the end of file from the client i switch to cgi_sending...
and i will do the opposite. check the cgi_out for pollin and read from the cgi BUFFER_SIZE and send to the client after checking he is pollout/ This until i get the eof from the cgi...