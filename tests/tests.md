## pytest

To test locally you need to change directory to the `tests` folder and run the following command:

```python
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

then 
```python
pytest integration
```
or just `pytest` to run all tests.
